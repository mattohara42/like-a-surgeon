# Assumptions

Decisions made without asking, logged per METHOD.md #8.

- 2026-09-21: The task description was the full text of a methodology
  write-up (design principles for working with a coding agent on a
  large, prose-heavy project). The repository was empty (no commits, no
  remote branches). Interpreted the task as: commit this write-up as
  `METHOD.md` at the repo root, since it is explicitly framed as
  something to be "written down so the pattern is reusable." Did not
  scaffold the other files it references (`CLAUDE.md`, `data/seed.json`,
  `QUESTIONS.md`, `BACKLOG.md`, `prompts/`) since none of their content
  was provided and inventing them would be guessing at the actual
  project brief.
